<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:LifTools.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:dg="using:Avalonia.Controls"
        xmlns:models="using:LifTools.Models"
        xmlns:converters="using:LifTools.Converters"
        mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="800"
        x:Class="LifTools.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Title="LifTools">

    <Design.DataContext>
        <!-- This only sets the DataContext for the previewer in an IDE,
             to set the actual DataContext for runtime, set the DataContext property in code (look at App.axaml.cs) -->
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <Window.Resources>
        <converters:TimeFormatConverter x:Key="TimeFormatConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- File Selection -->
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="LIF File:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding SelectedFilePath}" 
                     VerticalAlignment="Center"
                     Margin="0,0,10,0"
                     IsReadOnly="True"/>
            <Button Grid.Column="2" Content="Browse..." 
                    Click="BrowseButton_Click"
                    VerticalAlignment="Center"/>
        </Grid>
        
        <!-- Time Format Toggle -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10" HorizontalAlignment="Right">
            <TextBlock Text="Time Format:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <RadioButton Content="Seconds" 
                        IsChecked="{Binding IsRawTimeFormat}" 
                        GroupName="TimeFormat"
                        VerticalAlignment="Center"
                        Margin="0,0,10,0"
                        Click="RawTimeFormat_Click"/>
            <RadioButton Content="MM:SS.sss" 
                        IsChecked="{Binding IsFormattedTimeFormat}" 
                        GroupName="TimeFormat"
                        VerticalAlignment="Center"
                        Click="FormattedTimeFormat_Click"/>
        </StackPanel>
        
        <!-- Race Information -->
        <Border Grid.Row="2" 
                BorderBrush="LightGray" 
                BorderThickness="1" 
                Margin="10,0,10,10"
                Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*" MinHeight="250"/>
                </Grid.RowDefinitions>
                
                <!-- Race Info Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="{Binding CurrentRace.RaceInfo}" 
                               FontWeight="Bold" 
                               FontSize="14"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding Racers.Count, StringFormat='Racers: {0}'}" 
                               FontSize="12" 
                               Foreground="Gray"/>
                </StackPanel>
                
                <!-- DataGrid for Racers -->
                <Grid Grid.Row="1">
                        <dg:DataGrid ItemsSource="{Binding Racers}"
                                     AutoGenerateColumns="False"
                                     CanUserReorderColumns="True"
                                     CanUserResizeColumns="True"
                                     CanUserSortColumns="True"
                                     GridLinesVisibility="Horizontal"
                                     HeadersVisibility="Column"
                                     IsReadOnly="True"
                                     Background="White"
                                     BorderBrush="LightGray"
                                     BorderThickness="1">
                            <dg:DataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Copy Selected Cell" Command="{Binding CopySelectedCellCommand}"/>
                                    <MenuItem Header="Copy Selected Row" Command="{Binding CopySelectedRowCommand}"/>
                                    <MenuItem Header="Copy All Data" Command="{Binding CopyAllDataCommand}"/>
                                </ContextMenu>
                            </dg:DataGrid.ContextMenu>
                        <dg:DataGrid.Columns>
                            <DataGridTextColumn Header="Pos" Binding="{Binding Position}" Width="80" SortMemberPath="Position"/>
                            <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="130"/>
                            <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="130"/>
                            <DataGridTextColumn Header="Affiliation" Binding="{Binding Affiliation}" Width="180"/>
                            <DataGridTemplateColumn Header="Finish Time" Width="130" SortMemberPath="FinishTime">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding DisplayFinishTime}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                            <Button Grid.Column="1" 
                                                    Width="20" 
                                                    Height="20" 
                                                    Padding="2"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Click="CopyFinishTime_Click"
                                                    Tag="{Binding DisplayFinishTime}"
                                                    ToolTip.Tip="Copy">
                                                <Button.Content>
                                                    <TextBlock Text="📋" FontSize="12"/>
                                                </Button.Content>
                                            </Button>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Lane" Binding="{Binding LineNumber}" Width="80"/>
                            <DataGridTextColumn Header="ID" Binding="{Binding RacerId}" Width="90"/>
                            <DataGridTextColumn Header="License" Binding="{Binding License}" Width="110"/>
                            <DataGridTextColumn Header="Delta" Binding="{Binding DeltaTime}" Width="110"/>
                            <DataGridTextColumn Header="Reaction" Binding="{Binding ReacTime}" Width="110"/>
                            <DataGridTextColumn Header="Splits" Binding="{Binding Splits}" Width="300"/>
                        </dg:DataGrid.Columns>
                    </dg:DataGrid>
                </Grid>
            </Grid>
        </Border>
        
        <!-- Status Bar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="10">
            <Button Content="Close" 
                    HorizontalAlignment="Right"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>

</Window>
